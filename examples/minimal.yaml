service:
  version: 0.3.0
  name: dummy
http:
  port: 8080
  endpoint: api
dbgroup:
  read:
    connString: >-
      host=localhost port=5432 dbname=postgres user=postgres password=postgres
      sslmode=disable
    schema: api
